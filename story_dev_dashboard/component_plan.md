# Component plan

Canon status: PROPOSED

## MVP

- Single page component: server-rendered, reads JSON via `fs`.
- Styling: Tailwind utility classes consistent with `app/read/page.tsx` tone (stone/amber).

## Later (optional)

- Split into `StoryDevHeader`, `StoryDevProgressCard`, `StoryDevListSection` if the page grows.
- Add a tiny `StoryDevBadge` for sweep / research status chips.
