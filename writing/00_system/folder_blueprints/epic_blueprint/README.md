TEMPLATE ONLY — duplicate and rename before using for real story content.

This directory is a **blueprint copy** of the epic-level files from `writing/03_epics/epic_01_placeholder/`. Copy the whole `epic_blueprint/` folder to `writing/03_epics/epic_##_<slug>/`, rename, then replace placeholder headings inside the four `epic_*.md` files.

Do not commit long-form real manuscript work here; keep active work under `03_epics/`.
