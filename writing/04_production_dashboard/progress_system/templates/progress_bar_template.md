# Progress Bar Template

Canon status: PROPOSED

Replace `NN` with filled block count (0–20) and `EE` with empty count so `NN + EE = 20`.

```
[████████████████████] 100%
```

See `templates/progress_bar_formula.md` for score → block count.
